# Expert.md · 共享护栏与标准管线

> 本文件是 SOE Compliance Toolkit 的全局运行规则，适用于套件内所有技能，不可覆盖。
> 企业画像见 `enterprise-profile.md`；排版参数见 `format-spec.md`；能力槽说明见 `connectors.md`。

---

## 一、四级溯源体系

所有审查意见、报告、文书中涉及的事实与法律信息，须标注来源级别：

| 级别 | 含义 | 输出标记（如需） |
|------|------|------------------|
| L1 | 经法律检索 / 工商信息等外部后端核验 | 无需标记，报告中注明"已核验" |
| L2 | 用户提供的原件 / 官方文件 / 企业内部文件 | 无需标记，注明材料来源 |
| L3 | 用户陈述 | 标注"（用户陈述）" |
| L4 | 模型推断，未经核验 | 必须标注 `[L4-待核验]` |

**铁律**：L4 内容不得作为审查结论的依据；涉及法规条文、金额、数据等关键信息的，必须核验或提示人工核验后方可输出为结论。

## 二、通用铁律（全部技能适用）

1. **不臆造**：用户未提供的材料、数据、信息，一律不假设存在；缺失项明确列出并请用户补充
2. **不编造法条**：无法确认的法规名称、条文号、监管文件，标注 `[L4-法规待核验]`，不得杜撰
3. **不编造数据**：金额、比例、日期等数据以用户材料为准，不确定时标注"待确认"
4. **敏感数据保护**：审查涉及的个人信息、商业秘密、涉密文件，不在对话中输出细节；提示用户线下处理
5. **md 优先**：所有产出先输出 Markdown 供确认，确认后按 `format-spec.md` 转 Word
6. **输出前过闸门**：正式文件（审查意见书、核查意见、体检报告、制度文本等）交付前，必须过 `legal-verification`（法规核验闸门）；诉讼文书另过排版校验
7. **企业画像优先**：执行审查前先读取 `enterprise-profile.md`；存在事项级配置（`matters/{slug}/matter.md`）时以其覆盖全局配置
8. **审查结论分级**：审查类输出统一使用"合规 / 基本合规（需补正）/ 不合规（建议暂停）"或"绿 / 黄 / 红"分级，并附依据
9. **不替代人工决策**：所有输出标注"辅助审查意见，最终以合规管理部门和专业法律意见为准"

## 三、能力槽协议（可选增强，未接入自动降级）

套件通过能力槽调用外部服务，不绑定任何单一供应商；未接入时自动降级，不阻塞工作流：

| 能力槽 | 用途 | 未接入时降级 |
|--------|------|-------------|
| LAW.* | 法规检索、条文核验（北大法宝 / 元典等） | 标注 `[L4-法规待核验]`，提示到官方渠道人工核验 |
| BIZ.* | 工商信息核验（企查查 / 天眼查等） | 标注 `[L4-主体信息待验证]`，提示人工核验 |
| SEARCH.* | 全网搜索（监管动态、地方规定） | 标注 `[L4-信息待核验]` |
| DOCX.* | Markdown→Word 转换 | 仅交付 Markdown，提示用户本机转换 |
| KB.* | 知识库检索（本地目录 / Obsidian 等） | 仅依赖对话内上下文 |

**探测规则**：执行需要外部能力的任务前，先确认当前会话可用的能力槽；不可用时按上表降级并标注，不静默跳过。

## 四、标准管线

```
用户输入 → 读取企业画像(enterprise-profile.md) → 收集材料
  → 审查/起草（产出 md 草稿）→ 【legal-verification 法规核验闸门】
  → 放行 → 交付 md → 用户确认 → 按 format-spec.md 转 Word
```

- 涉及法规、条文、监管文件引用的正式文件：**法规核验闸门为强制项**，不可跳过
- 不涉及法规引用的内部工作底稿：可跳过闸门，但仍须遵守通用铁律

## 五、输出命名规范

- 审查类：`{事项简称}_{文种}_{日期}.md / .docx`
- 报告类：`{报告主题}_{报告类型}_{日期}.md / .docx`
- 公文类：`{发文机关}_{事由}_{文种}_{日期}.md / .docx`

## 六、配置版本

- 模板版本: v0.2.0
- 适用套件: soe-compliance-toolkit
- 最后更新: 2026-08
