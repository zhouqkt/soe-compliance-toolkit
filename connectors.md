# connectors.md · 能力槽连接器说明

> 本套件设计为**无连接器即可完整使用**。连接器为可选增强，接入后能力更强。
> 能力槽协议见 `Expert.md`；企业画像见 `enterprise-profile.md`。

## 设计原则

1. **无连接器可用**：所有技能在无任何外部连接时仍可正常工作，仅能力降级并标注
2. **优雅降级**：每个连接器不可用时，技能自动切换降级方案并标注（`[L4-待核验]` 等）
3. **平台无关**：技能通过能力槽（LAW.* / BIZ.* / SEARCH.* / DOCX.* / KB.*）调用外部服务，不绑定任何单一供应商
4. **按需接入**：用户根据实际权限逐步接入，无需一步到位

## 能力槽一览

| 能力槽 | 用途 | 可选供应商 | 未接入时降级 |
|--------|------|-----------|-------------|
| LAW.* | 法规检索、条文核验 | 北大法宝 / 元典 / 其他法律数据库 MCP | 标注 `[L4-法规待核验]`，提示官方渠道人工核验 |
| BIZ.* | 工商信息核验（股权、关联、涉诉） | 企查查 / 天眼查 / 启信宝 | 标注 `[L4-主体信息待验证]`，提示人工核验 |
| SEARCH.* | 全网搜索（监管动态、地方规定） | 任意搜索类 MCP | 标注 `[L4-信息待核验]` |
| DOCX.* | Markdown→Word 转换 | 套件内置 `scripts/md2docx_gbt.py` | 仅交付 Markdown |
| KB.* | 知识库检索（法规库/案例/笔记） | 本地目录 / Obsidian / 云端知识库 | 仅依赖对话内上下文 |

## 推荐配置

### 最小配置（个人/内部试用）
无需任何连接器。所有技能以本地模式运行，法规引用标注 `[L4-法规待核验]`。

### 推荐配置（正式使用）
- **LAW.***：北大法宝或元典任一（法规核验闸门效果最大化）
- **BIZ.***：企查查或同类（贸易真实性、关联交易核验）
- **KB.***：本地目录（沉淀审查经验，见 compliance-knowledge-base）

### 完整配置
全部接入：LAW.* + BIZ.* + SEARCH.* + KB.*（多供应商灾备）。

## 配置方式

在 `enterprise-profile.md` 的「外部能力后端」中登记实际接入的能力槽：
```yaml
LAW.*: 北大法宝
BIZ.*: 企查查
KB.*: 本地目录（/path/to/kb）
```

技能运行时读取画像，按已接入能力槽调用；未接入的走降级。
